#include "Menu.h"

void Menu::display(Player *player, Map *map) {
/*
  int x, y;
  player->locate(&y, &x);
  int energy = player->getEnergy();
  int money = player->getMoney();
*/
  mvvline(0, COLS - MENU_WIDTH, MENU_BORDER, LINES);
}
